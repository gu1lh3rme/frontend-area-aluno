/**
 * Configurações de ambiente para produção.
 * Altere a apiUrl para a URL do seu backend FastAPI em produção.
 */
export const environment = {
  production: true,
  // URL base do backend FastAPI em produção
  apiUrl: 'https://api.seudominio.com'
};
